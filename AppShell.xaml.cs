namespace Tetris;

// Définition partielle de la classe AppShell qui hérite de Shell
public partial class AppShell : Shell
{
    // Constructeur de la classe AppShell
    public AppShell()
    {
        // Initialisation des composants de l'interface utilisateur
        InitializeComponent();
    }
}
